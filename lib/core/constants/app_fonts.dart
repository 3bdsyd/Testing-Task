/// Font families bundled with the app. Only the Nunito weights the design
/// uses are shipped (Regular/Bold/ExtraBold) — see `pubspec.yaml`.
abstract final class AppFonts {
  const AppFonts._();

  static const String nunito = 'Nunito';
}
